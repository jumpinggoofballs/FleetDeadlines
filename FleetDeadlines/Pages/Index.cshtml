@page
@model IndexModel
@{
    ViewData["Title"] = "Fleet Deadlines";
}

<div class="text-center">
    <h1 class="display-4">Welcome</h1>
</div>

@foreach (var item in Model.Vehicles)
{
    <li>
        @item.RegistrationNumber
    </li>
}